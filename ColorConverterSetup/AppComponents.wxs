<Wix xmlns="http://wixtoolset.org/schemas/v4/wxs">
	<Fragment>
		<ComponentGroup Id="AppComponents" Directory="INSTALLFOLDER">
			<Files Include="$(ColorConverter.TargetDir)\**">
				<Exclude Files="$(ColorConverter.TargetDir)\*.pdb" />
			</Files>
		</ComponentGroup>
	</Fragment>
</Wix>
